# Raeef Kabbani Profile

This is a profile page for Raeef Kabbani, created based on a provided image design.

## How to run locally

You can serve this website locally using Python:

```bash
python3 -m http.server
```

Then visit `http://localhost:8000`.
